name: Mon Ping Automatique

on:
  schedule:
    - cron: '*/10 * * * *' # Toutes les 10 minutes
  workflow_dispatch:      # Permet de tester manuellement

jobs:
  check-site:
    runs-on: ubuntu-latest
    steps:
      - name: Ping du site
        run: curl -I -s https://onaanpizza.onrender.com
